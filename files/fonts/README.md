# Local font files

Put local, user-supplied font files here when ArchDevKit needs to install fonts that should not be downloaded from a public URL.

For Monaco, use:

```bash
files/fonts/Monaco_Linux.ttf
```

This directory is intentionally kept in the repository, but proprietary font files should stay local and should not be committed.
